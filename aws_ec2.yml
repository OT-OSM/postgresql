# AWS Dynamic Inventory Setup
---
plugin: aws_ec2
regions:
    - us-east-1

filters:
  tag:Name:
    - 'PG-SQL-master'
    - 'PG-SQL-Slave-1'
    - 'PG-SQL-Slave-2'

compose:
    ansible_host: private_ip_address
...
